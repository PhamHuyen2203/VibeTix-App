<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="16dp"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/ivOrgSwitchLogo"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_organizer_placeholder"
        android:background="@drawable/bg_circle_white_alpha"
        android:scaleType="centerCrop" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvOrgSwitchName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ban tổ chức"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/clr_text_black" />

        <TextView
            android:id="@+id/tvOrgSwitchSub"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nhấn để chọn"
            android:textSize="12sp"
            android:textColor="@color/clr_text_secondary" />
    </LinearLayout>

    <ImageView
        android:id="@+id/ivActiveCheck"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_chevron_right"
        android:tint="@color/clr_primary"
        android:visibility="gone" />

</LinearLayout>
