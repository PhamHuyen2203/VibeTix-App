package com.example.vibetix.Activities.User;

import android.os.Bundle;
import android.widget.Toast;

import androidx.appcompat.app.AppCompatActivity;

/**
 * OrganizerProfilesActivity — Quản lý danh sách hồ sơ Ban tổ chức của user.
 * Stub: tính năng sẽ được phát triển đầy đủ sau.
 */
public class OrganizerProfilesActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Chưa có layout riêng — dùng Toast tạm thời
        Toast.makeText(this, "Quản lý hồ sơ Ban tổ chức (sắp ra mắt)", Toast.LENGTH_SHORT).show();
        finish();
    }
}
